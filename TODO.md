# HexSBM — Планы и улучшения

## 🔧 Конфигурация и UI

### 1. Добавить поддержку "Без градиента"
- **Файл**: `HexSBMConfig.java`
  - Добавить поле: `public boolean disableGradient = false;`
  - Добавить геттер/сеттер.
- **Файл**: `ColorScheme.java`
  - Изменить логику рендера цветов: если `disableGradient == true`, использовать сплошной цвет вместо перехода.
- **Файл**: `ConfigPanel.java`
  - Заменить заглушку `() -> false, v -> {}` на реальные геттер/сеттер.

### 2. Переработать валидацию и взаимосвязь колец
- **Проблема**: сейчас радиусы колец "выталкивают" друг друга через `enforceRingOrder()`, но логика жёсткая и неудобная.
- **Цель**: сделать более гибкую систему, где изменение одного радиуса **плавно подстраивает** соседние, но не ломает UI.
- **Файлы**:
  - `HexSBMConfig.java` → переписать `enforceRingOrder()`
  - `ConfigPanel.java` → убедиться, что изменения мгновенно отражаются.

### 3. Улучшить визуальную обратную связь в NumberField
- Показывать, когда поле редактируется (например, рамка или цвет фона).
- Отключать прокрутку колёсиком, пока идёт ручной ввод.

---

## 🌍 Локализация (i18n)

### 4. Добавить поддержку локализации
- Заменить все `Text.literal("...")` в UI на `Text.translatable("key")`.
- Создать `assets/hexsbm/lang/ru_ru.json`, `en_us.json`.
- Примеры ключей:
  - `hexsbm.config.label.outer_ring`
  - `hexsbm.config.toggle.tooltips`
  - `hexsbm.button.reset_to_default`
- **Файлы для правки**:
  - `ConfigPanel.java` (все надписи в `LabelControl`, `Button` и т.д.)
  - `SpellBookScreen.java` (если есть текстовые подсказки).

---

## 🗂 Архитектура и организация кода

### 5. Вынести UI-элементы в отдельную папку
- **Сейчас**: `src/client/java/com/hexsbm/screen/ui/` содержит 9 файлов.
- **Нужно**: создать подпапку `elements`:
    src/client/java/com/hexsbm/screen/ui/elements/
    ├── Button.java
    ├── CycleField.java
    ├── LabelControl.java
    ├── NumberField.java
    ├── ToggleField.java
    └── ConfigControl.java ← базовый класс
- **Оставить в `ui/`**:
- `ColorScheme.java`
- `RadialRenderer.java`
- `ConfigPanel.java`
- Обновить `import` во всех файлах.

---

## 🧼 Код-качество и поддержка

### 6. Финальная чистка кодовой базы
- **Цель**: сделать код лаконичным, читаемым и приятным для поддержки.
- **Что сделать**:
- Удалить весь мёртвый код (неиспользуемые поля, методы, импорты).
- Устранить дублирование логики (вынести общие операции в утилиты или базовые классы).
- Упростить сложные методы (разбить на подпрограммы с говорящими именами).
- Привести стиль к единому виду (пробелы, отступы, порядок полей).
- Убедиться, что каждый файл имеет **чёткую ответственность** (Single Responsibility).
- **Когда**: после завершения всех фич и рефакторингов.
- **Результат**: «Душа радуется, когда смотришь на код».

---

## ✨ Дополнительно (когда будет время)

- Рассмотреть возможность привязки цвета к активному заклинанию (а не только к пигменту).
- Добавить "режим разработчика" — быстрый сброс + лог изменений.
- Оптимизировать перерисовку: не пересоздавать `ColorScheme` при каждом фрейме, если цвет не менялся.